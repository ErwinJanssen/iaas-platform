"""Provider Abstraction Layer.

This package provides a unified interface for integrating with multiple cloud providers.
Each provider (Hetzner, AWS, DigitalOcean, etc.) has its own adapter that implements
the common ProviderInterface.
"""

__version__ = "0.1.0"
