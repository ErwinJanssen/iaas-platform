# Core dependencies for IaaS Platform - Hybrid Architecture
# We use existing tools (Crossplane, NATS, etc.) for commodity functionality

# Web Framework
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Async
anyio>=4.0.0
httpx>=0.25.0

# Data Validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
email-validator>=2.0.0

# Database
sqlalchemy>=2.0.20
asyncpg>=0.28.0
alembic>=1.12.0

# Redis
redis>=5.0.0

# Message Queue (NATS instead of RabbitMQ)
nats-py>=2.0.0

# Authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Kubernetes/Crossplane (for provider abstraction)
kubernetes>=28.0.0

# Object Storage (MinIO)
minio>=7.1.15

# Monitoring
prometheus-client>=0.19.0

# Logging
structlog>=23.2.0

# CLI
typer>=0.9.0
rich>=13.7.0

# Utilities
python-dotenv>=1.0.0
pytz>=2023.3
